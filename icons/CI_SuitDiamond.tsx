
"use client";
import React from "react";
import {IconProps, SuitDiamond} from "react-bootstrap-icons";

export const CI_SuitDiamond : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <SuitDiamond {...props}/>
        </>
    );
}
