
"use client";
import React from "react";
import {IconProps, SkipEndCircle} from "react-bootstrap-icons";

export const CI_SkipEndCircle : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <SkipEndCircle {...props}/>
        </>
    );
}
