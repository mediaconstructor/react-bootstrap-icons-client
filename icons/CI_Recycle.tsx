
"use client";
import React from "react";
import {IconProps, Recycle} from "react-bootstrap-icons";

export const CI_Recycle : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Recycle {...props}/>
        </>
    );
}
