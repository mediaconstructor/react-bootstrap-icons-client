
"use client";
import React from "react";
import {IconProps, XSquareFill} from "react-bootstrap-icons";

export const CI_XSquareFill : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <XSquareFill {...props}/>
        </>
    );
}
