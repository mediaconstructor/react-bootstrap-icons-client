
"use client";
import React from "react";
import {IconProps, ArrowClockwise} from "react-bootstrap-icons";

export const CI_ArrowClockwise : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <ArrowClockwise {...props}/>
        </>
    );
}
