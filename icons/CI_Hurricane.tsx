
"use client";
import React from "react";
import {IconProps, Hurricane} from "react-bootstrap-icons";

export const CI_Hurricane : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Hurricane {...props}/>
        </>
    );
}
