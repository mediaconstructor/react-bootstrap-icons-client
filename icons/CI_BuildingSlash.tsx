
"use client";
import React from "react";
import {IconProps, BuildingSlash} from "react-bootstrap-icons";

export const CI_BuildingSlash : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <BuildingSlash {...props}/>
        </>
    );
}
