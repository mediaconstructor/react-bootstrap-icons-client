
"use client";
import React from "react";
import {IconProps, Broadcast} from "react-bootstrap-icons";

export const CI_Broadcast : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Broadcast {...props}/>
        </>
    );
}
