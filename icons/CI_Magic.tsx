
"use client";
import React from "react";
import {IconProps, Magic} from "react-bootstrap-icons";

export const CI_Magic : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Magic {...props}/>
        </>
    );
}
