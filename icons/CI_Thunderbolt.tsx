
"use client";
import React from "react";
import {IconProps, Thunderbolt} from "react-bootstrap-icons";

export const CI_Thunderbolt : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Thunderbolt {...props}/>
        </>
    );
}
