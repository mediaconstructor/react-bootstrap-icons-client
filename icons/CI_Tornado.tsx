
"use client";
import React from "react";
import {IconProps, Tornado} from "react-bootstrap-icons";

export const CI_Tornado : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Tornado {...props}/>
        </>
    );
}
