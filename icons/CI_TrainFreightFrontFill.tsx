
"use client";
import React from "react";
import {IconProps, TrainFreightFrontFill} from "react-bootstrap-icons";

export const CI_TrainFreightFrontFill : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <TrainFreightFrontFill {...props}/>
        </>
    );
}
