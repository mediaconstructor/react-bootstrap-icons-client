
"use client";
import React from "react";
import {IconProps, Shift} from "react-bootstrap-icons";

export const CI_Shift : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Shift {...props}/>
        </>
    );
}
