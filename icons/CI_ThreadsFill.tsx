
"use client";
import React from "react";
import {IconProps, ThreadsFill} from "react-bootstrap-icons";

export const CI_ThreadsFill : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <ThreadsFill {...props}/>
        </>
    );
}
