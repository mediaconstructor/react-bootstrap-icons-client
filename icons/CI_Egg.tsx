
"use client";
import React from "react";
import {IconProps, Egg} from "react-bootstrap-icons";

export const CI_Egg : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Egg {...props}/>
        </>
    );
}
