
"use client";
import React from "react";
import {IconProps, SkipStartCircle} from "react-bootstrap-icons";

export const CI_SkipStartCircle : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <SkipStartCircle {...props}/>
        </>
    );
}
