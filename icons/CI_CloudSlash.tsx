
"use client";
import React from "react";
import {IconProps, CloudSlash} from "react-bootstrap-icons";

export const CI_CloudSlash : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <CloudSlash {...props}/>
        </>
    );
}
