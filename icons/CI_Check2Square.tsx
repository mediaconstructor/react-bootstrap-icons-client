
"use client";
import React from "react";
import {IconProps, Check2Square} from "react-bootstrap-icons";

export const CI_Check2Square : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Check2Square {...props}/>
        </>
    );
}
