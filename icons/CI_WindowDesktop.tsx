
"use client";
import React from "react";
import {IconProps, WindowDesktop} from "react-bootstrap-icons";

export const CI_WindowDesktop : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <WindowDesktop {...props}/>
        </>
    );
}
