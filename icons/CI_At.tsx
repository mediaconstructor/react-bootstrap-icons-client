
"use client";
import React from "react";
import {IconProps, At} from "react-bootstrap-icons";

export const CI_At : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <At {...props}/>
        </>
    );
}
