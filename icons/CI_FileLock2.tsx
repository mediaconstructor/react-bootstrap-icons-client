
"use client";
import React from "react";
import {IconProps, FileLock2} from "react-bootstrap-icons";

export const CI_FileLock2 : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <FileLock2 {...props}/>
        </>
    );
}
