
"use client";
import React from "react";
import {IconProps, BookmarkDash} from "react-bootstrap-icons";

export const CI_BookmarkDash : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <BookmarkDash {...props}/>
        </>
    );
}
