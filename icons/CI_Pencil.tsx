
"use client";
import React from "react";
import {IconProps, Pencil} from "react-bootstrap-icons";

export const CI_Pencil : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Pencil {...props}/>
        </>
    );
}
