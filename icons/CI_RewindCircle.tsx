
"use client";
import React from "react";
import {IconProps, RewindCircle} from "react-bootstrap-icons";

export const CI_RewindCircle : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <RewindCircle {...props}/>
        </>
    );
}
