
"use client";
import React from "react";
import {IconProps, RCircle} from "react-bootstrap-icons";

export const CI_RCircle : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <RCircle {...props}/>
        </>
    );
}
