
"use client";
import React from "react";
import {IconProps, StackOverflow} from "react-bootstrap-icons";

export const CI_StackOverflow : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <StackOverflow {...props}/>
        </>
    );
}
