
"use client";
import React from "react";
import {IconProps, Ubuntu} from "react-bootstrap-icons";

export const CI_Ubuntu : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Ubuntu {...props}/>
        </>
    );
}
