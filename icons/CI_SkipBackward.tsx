
"use client";
import React from "react";
import {IconProps, SkipBackward} from "react-bootstrap-icons";

export const CI_SkipBackward : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <SkipBackward {...props}/>
        </>
    );
}
