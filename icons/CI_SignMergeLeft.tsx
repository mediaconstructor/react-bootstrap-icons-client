
"use client";
import React from "react";
import {IconProps, SignMergeLeft} from "react-bootstrap-icons";

export const CI_SignMergeLeft : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <SignMergeLeft {...props}/>
        </>
    );
}
