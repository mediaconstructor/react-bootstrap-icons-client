
"use client";
import React from "react";
import {IconProps, Alt} from "react-bootstrap-icons";

export const CI_Alt : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Alt {...props}/>
        </>
    );
}
