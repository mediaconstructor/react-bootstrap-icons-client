
"use client";
import React from "react";
import {IconProps, SignTurnLeft} from "react-bootstrap-icons";

export const CI_SignTurnLeft : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <SignTurnLeft {...props}/>
        </>
    );
}
