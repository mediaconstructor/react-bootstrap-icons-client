
"use client";
import React from "react";
import {IconProps, TrainFreightFront} from "react-bootstrap-icons";

export const CI_TrainFreightFront : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <TrainFreightFront {...props}/>
        </>
    );
}
