
"use client";
import React from "react";
import {IconProps, FastForward} from "react-bootstrap-icons";

export const CI_FastForward : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <FastForward {...props}/>
        </>
    );
}
