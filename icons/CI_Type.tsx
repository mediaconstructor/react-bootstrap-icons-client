
"use client";
import React from "react";
import {IconProps, Type} from "react-bootstrap-icons";

export const CI_Type : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Type {...props}/>
        </>
    );
}
