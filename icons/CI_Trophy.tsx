
"use client";
import React from "react";
import {IconProps, Trophy} from "react-bootstrap-icons";

export const CI_Trophy : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Trophy {...props}/>
        </>
    );
}
