
"use client";
import React from "react";
import {IconProps, Box2} from "react-bootstrap-icons";

export const CI_Box2 : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Box2 {...props}/>
        </>
    );
}
