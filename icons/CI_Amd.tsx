
"use client";
import React from "react";
import {IconProps, Amd} from "react-bootstrap-icons";

export const CI_Amd : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Amd {...props}/>
        </>
    );
}
