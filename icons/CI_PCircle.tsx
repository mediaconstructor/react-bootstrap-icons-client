
"use client";
import React from "react";
import {IconProps, PCircle} from "react-bootstrap-icons";

export const CI_PCircle : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <PCircle {...props}/>
        </>
    );
}
