
"use client";
import React from "react";
import {IconProps, BrowserEdge} from "react-bootstrap-icons";

export const CI_BrowserEdge : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <BrowserEdge {...props}/>
        </>
    );
}
