
"use client";
import React from "react";
import {IconProps, LayerBackward} from "react-bootstrap-icons";

export const CI_LayerBackward : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <LayerBackward {...props}/>
        </>
    );
}
