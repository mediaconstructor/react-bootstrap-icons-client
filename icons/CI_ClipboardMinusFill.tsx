
"use client";
import React from "react";
import {IconProps, ClipboardMinusFill} from "react-bootstrap-icons";

export const CI_ClipboardMinusFill : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <ClipboardMinusFill {...props}/>
        </>
    );
}
