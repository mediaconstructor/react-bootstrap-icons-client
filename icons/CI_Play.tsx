
"use client";
import React from "react";
import {IconProps, Play} from "react-bootstrap-icons";

export const CI_Play : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Play {...props}/>
        </>
    );
}
