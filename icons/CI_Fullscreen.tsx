
"use client";
import React from "react";
import {IconProps, Fullscreen} from "react-bootstrap-icons";

export const CI_Fullscreen : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Fullscreen {...props}/>
        </>
    );
}
