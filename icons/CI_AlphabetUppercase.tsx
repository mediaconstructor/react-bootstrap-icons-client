
"use client";
import React from "react";
import {IconProps, AlphabetUppercase} from "react-bootstrap-icons";

export const CI_AlphabetUppercase : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <AlphabetUppercase {...props}/>
        </>
    );
}
