
"use client";
import React from "react";
import {IconProps, DatabaseSlash} from "react-bootstrap-icons";

export const CI_DatabaseSlash : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <DatabaseSlash {...props}/>
        </>
    );
}
