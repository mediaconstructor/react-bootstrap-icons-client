
"use client";
import React from "react";
import {IconProps, Power} from "react-bootstrap-icons";

export const CI_Power : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Power {...props}/>
        </>
    );
}
