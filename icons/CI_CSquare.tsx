
"use client";
import React from "react";
import {IconProps, CSquare} from "react-bootstrap-icons";

export const CI_CSquare : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <CSquare {...props}/>
        </>
    );
}
