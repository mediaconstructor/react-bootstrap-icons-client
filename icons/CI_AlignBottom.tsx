
"use client";
import React from "react";
import {IconProps, AlignBottom} from "react-bootstrap-icons";

export const CI_AlignBottom : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <AlignBottom {...props}/>
        </>
    );
}
