
"use client";
import React from "react";
import {IconProps, Explicit} from "react-bootstrap-icons";

export const CI_Explicit : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Explicit {...props}/>
        </>
    );
}
