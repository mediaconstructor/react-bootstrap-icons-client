
"use client";
import React from "react";
import {IconProps, Tsunami} from "react-bootstrap-icons";

export const CI_Tsunami : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Tsunami {...props}/>
        </>
    );
}
