
"use client";
import React from "react";
import {IconProps, PcHorizontal} from "react-bootstrap-icons";

export const CI_PcHorizontal : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <PcHorizontal {...props}/>
        </>
    );
}
