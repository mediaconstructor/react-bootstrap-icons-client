
"use client";
import React from "react";
import {IconProps, ClipboardCheckFill} from "react-bootstrap-icons";

export const CI_ClipboardCheckFill : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <ClipboardCheckFill {...props}/>
        </>
    );
}
