
"use client";
import React from "react";
import {IconProps, TerminalDash} from "react-bootstrap-icons";

export const CI_TerminalDash : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <TerminalDash {...props}/>
        </>
    );
}
