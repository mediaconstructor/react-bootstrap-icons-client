
"use client";
import React from "react";
import {IconProps, RSquare} from "react-bootstrap-icons";

export const CI_RSquare : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <RSquare {...props}/>
        </>
    );
}
