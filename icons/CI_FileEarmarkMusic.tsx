
"use client";
import React from "react";
import {IconProps, FileEarmarkMusic} from "react-bootstrap-icons";

export const CI_FileEarmarkMusic : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <FileEarmarkMusic {...props}/>
        </>
    );
}
