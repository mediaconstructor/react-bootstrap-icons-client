
"use client";
import React from "react";
import {IconProps, BagDash} from "react-bootstrap-icons";

export const CI_BagDash : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <BagDash {...props}/>
        </>
    );
}
