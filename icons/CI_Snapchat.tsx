
"use client";
import React from "react";
import {IconProps, Snapchat} from "react-bootstrap-icons";

export const CI_Snapchat : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Snapchat {...props}/>
        </>
    );
}
