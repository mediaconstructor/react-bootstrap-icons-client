
"use client";
import React from "react";
import {IconProps, Tablet} from "react-bootstrap-icons";

export const CI_Tablet : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Tablet {...props}/>
        </>
    );
}
