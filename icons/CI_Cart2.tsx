
"use client";
import React from "react";
import {IconProps, Cart2} from "react-bootstrap-icons";

export const CI_Cart2 : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Cart2 {...props}/>
        </>
    );
}
