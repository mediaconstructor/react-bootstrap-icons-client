
"use client";
import React from "react";
import {IconProps, CameraReelsFill} from "react-bootstrap-icons";

export const CI_CameraReelsFill : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <CameraReelsFill {...props}/>
        </>
    );
}
