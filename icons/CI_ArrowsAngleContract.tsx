
"use client";
import React from "react";
import {IconProps, ArrowsAngleContract} from "react-bootstrap-icons";

export const CI_ArrowsAngleContract : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <ArrowsAngleContract {...props}/>
        </>
    );
}
