
"use client";
import React from "react";
import {IconProps, XCircle} from "react-bootstrap-icons";

export const CI_XCircle : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <XCircle {...props}/>
        </>
    );
}
