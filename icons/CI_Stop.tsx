
"use client";
import React from "react";
import {IconProps, Stop} from "react-bootstrap-icons";

export const CI_Stop : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Stop {...props}/>
        </>
    );
}
