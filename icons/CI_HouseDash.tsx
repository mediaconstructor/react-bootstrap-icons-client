
"use client";
import React from "react";
import {IconProps, HouseDash} from "react-bootstrap-icons";

export const CI_HouseDash : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <HouseDash {...props}/>
        </>
    );
}
