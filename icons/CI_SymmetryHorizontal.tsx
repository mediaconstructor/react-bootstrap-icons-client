
"use client";
import React from "react";
import {IconProps, SymmetryHorizontal} from "react-bootstrap-icons";

export const CI_SymmetryHorizontal : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <SymmetryHorizontal {...props}/>
        </>
    );
}
