
"use client";
import React from "react";
import {IconProps, MenuUp} from "react-bootstrap-icons";

export const CI_MenuUp : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <MenuUp {...props}/>
        </>
    );
}
