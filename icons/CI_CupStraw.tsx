
"use client";
import React from "react";
import {IconProps, CupStraw} from "react-bootstrap-icons";

export const CI_CupStraw : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <CupStraw {...props}/>
        </>
    );
}
