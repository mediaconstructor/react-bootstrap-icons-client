
"use client";
import React from "react";
import {IconProps, Rewind} from "react-bootstrap-icons";

export const CI_Rewind : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Rewind {...props}/>
        </>
    );
}
