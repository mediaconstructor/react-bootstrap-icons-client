
"use client";
import React from "react";
import {IconProps, CcCircle} from "react-bootstrap-icons";

export const CI_CcCircle : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <CcCircle {...props}/>
        </>
    );
}
