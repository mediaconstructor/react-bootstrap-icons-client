
"use client";
import React from "react";
import {IconProps, SignpostSplitFill} from "react-bootstrap-icons";

export const CI_SignpostSplitFill : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <SignpostSplitFill {...props}/>
        </>
    );
}
