
"use client";
import React from "react";
import {IconProps, AlignStart} from "react-bootstrap-icons";

export const CI_AlignStart : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <AlignStart {...props}/>
        </>
    );
}
