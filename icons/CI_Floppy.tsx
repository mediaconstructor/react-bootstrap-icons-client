
"use client";
import React from "react";
import {IconProps, Floppy} from "react-bootstrap-icons";

export const CI_Floppy : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Floppy {...props}/>
        </>
    );
}
