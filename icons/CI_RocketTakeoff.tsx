
"use client";
import React from "react";
import {IconProps, RocketTakeoff} from "react-bootstrap-icons";

export const CI_RocketTakeoff : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <RocketTakeoff {...props}/>
        </>
    );
}
