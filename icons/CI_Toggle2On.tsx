
"use client";
import React from "react";
import {IconProps, Toggle2On} from "react-bootstrap-icons";

export const CI_Toggle2On : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Toggle2On {...props}/>
        </>
    );
}
