
"use client";
import React from "react";
import {IconProps, Save2} from "react-bootstrap-icons";

export const CI_Save2 : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Save2 {...props}/>
        </>
    );
}
