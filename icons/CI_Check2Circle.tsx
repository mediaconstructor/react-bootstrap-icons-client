
"use client";
import React from "react";
import {IconProps, Check2Circle} from "react-bootstrap-icons";

export const CI_Check2Circle : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Check2Circle {...props}/>
        </>
    );
}
