
"use client";
import React from "react";
import {IconProps, Microsoft} from "react-bootstrap-icons";

export const CI_Microsoft : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Microsoft {...props}/>
        </>
    );
}
