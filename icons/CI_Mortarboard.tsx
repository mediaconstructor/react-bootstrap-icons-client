
"use client";
import React from "react";
import {IconProps, Mortarboard} from "react-bootstrap-icons";

export const CI_Mortarboard : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Mortarboard {...props}/>
        </>
    );
}
