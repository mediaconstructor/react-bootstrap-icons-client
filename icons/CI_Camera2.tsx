
"use client";
import React from "react";
import {IconProps, Camera2} from "react-bootstrap-icons";

export const CI_Camera2 : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Camera2 {...props}/>
        </>
    );
}
