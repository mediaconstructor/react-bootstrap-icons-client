
"use client";
import React from "react";
import {IconProps, Sun} from "react-bootstrap-icons";

export const CI_Sun : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Sun {...props}/>
        </>
    );
}
