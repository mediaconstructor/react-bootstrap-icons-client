
"use client";
import React from "react";
import {IconProps, SimSlash} from "react-bootstrap-icons";

export const CI_SimSlash : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <SimSlash {...props}/>
        </>
    );
}
