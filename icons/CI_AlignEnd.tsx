
"use client";
import React from "react";
import {IconProps, AlignEnd} from "react-bootstrap-icons";

export const CI_AlignEnd : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <AlignEnd {...props}/>
        </>
    );
}
