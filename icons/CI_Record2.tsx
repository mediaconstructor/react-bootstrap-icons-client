
"use client";
import React from "react";
import {IconProps, Record2} from "react-bootstrap-icons";

export const CI_Record2 : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Record2 {...props}/>
        </>
    );
}
