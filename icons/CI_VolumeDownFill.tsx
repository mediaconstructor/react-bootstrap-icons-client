
"use client";
import React from "react";
import {IconProps, VolumeDownFill} from "react-bootstrap-icons";

export const CI_VolumeDownFill : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <VolumeDownFill {...props}/>
        </>
    );
}
