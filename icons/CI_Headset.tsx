
"use client";
import React from "react";
import {IconProps, Headset} from "react-bootstrap-icons";

export const CI_Headset : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Headset {...props}/>
        </>
    );
}
