
"use client";
import React from "react";
import {IconProps, SuitClub} from "react-bootstrap-icons";

export const CI_SuitClub : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <SuitClub {...props}/>
        </>
    );
}
