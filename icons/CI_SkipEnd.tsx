
"use client";
import React from "react";
import {IconProps, SkipEnd} from "react-bootstrap-icons";

export const CI_SkipEnd : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <SkipEnd {...props}/>
        </>
    );
}
