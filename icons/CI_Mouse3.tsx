
"use client";
import React from "react";
import {IconProps, Mouse3} from "react-bootstrap-icons";

export const CI_Mouse3 : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Mouse3 {...props}/>
        </>
    );
}
