
"use client";
import React from "react";
import {IconProps, ArrowDown} from "react-bootstrap-icons";

export const CI_ArrowDown : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <ArrowDown {...props}/>
        </>
    );
}
