
"use client";
import React from "react";
import {IconProps, Front} from "react-bootstrap-icons";

export const CI_Front : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Front {...props}/>
        </>
    );
}
