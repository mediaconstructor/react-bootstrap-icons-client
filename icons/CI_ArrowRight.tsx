
"use client";
import React from "react";
import {IconProps, ArrowRight} from "react-bootstrap-icons";

export const CI_ArrowRight : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <ArrowRight {...props}/>
        </>
    );
}
