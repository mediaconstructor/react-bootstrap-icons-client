
"use client";
import React from "react";
import {IconProps, Google} from "react-bootstrap-icons";

export const CI_Google : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Google {...props}/>
        </>
    );
}
