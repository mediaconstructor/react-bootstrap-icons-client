
"use client";
import React from "react";
import {IconProps, TrainFront} from "react-bootstrap-icons";

export const CI_TrainFront : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <TrainFront {...props}/>
        </>
    );
}
