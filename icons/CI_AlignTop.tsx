
"use client";
import React from "react";
import {IconProps, AlignTop} from "react-bootstrap-icons";

export const CI_AlignTop : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <AlignTop {...props}/>
        </>
    );
}
