
"use client";
import React from "react";
import {IconProps, Nvidia} from "react-bootstrap-icons";

export const CI_Nvidia : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Nvidia {...props}/>
        </>
    );
}
