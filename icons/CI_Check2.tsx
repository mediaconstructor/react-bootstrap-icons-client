
"use client";
import React from "react";
import {IconProps, Check2} from "react-bootstrap-icons";

export const CI_Check2 : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Check2 {...props}/>
        </>
    );
}
