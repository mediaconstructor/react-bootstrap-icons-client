
"use client";
import React from "react";
import {IconProps, Windows} from "react-bootstrap-icons";

export const CI_Windows : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Windows {...props}/>
        </>
    );
}
