
"use client";
import React from "react";
import {IconProps, Briefcase} from "react-bootstrap-icons";

export const CI_Briefcase : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Briefcase {...props}/>
        </>
    );
}
