
"use client";
import React from "react";
import {IconProps, WindowDash} from "react-bootstrap-icons";

export const CI_WindowDash : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <WindowDash {...props}/>
        </>
    );
}
