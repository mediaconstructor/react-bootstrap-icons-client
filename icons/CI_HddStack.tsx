
"use client";
import React from "react";
import {IconProps, HddStack} from "react-bootstrap-icons";

export const CI_HddStack : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <HddStack {...props}/>
        </>
    );
}
