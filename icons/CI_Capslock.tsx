
"use client";
import React from "react";
import {IconProps, Capslock} from "react-bootstrap-icons";

export const CI_Capslock : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Capslock {...props}/>
        </>
    );
}
