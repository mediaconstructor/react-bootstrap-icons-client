
"use client";
import React from "react";
import {IconProps, Key} from "react-bootstrap-icons";

export const CI_Key : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Key {...props}/>
        </>
    );
}
