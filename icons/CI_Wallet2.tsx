
"use client";
import React from "react";
import {IconProps, Wallet2} from "react-bootstrap-icons";

export const CI_Wallet2 : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Wallet2 {...props}/>
        </>
    );
}
