
"use client";
import React from "react";
import {IconProps, HandIndexThumb} from "react-bootstrap-icons";

export const CI_HandIndexThumb : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <HandIndexThumb {...props}/>
        </>
    );
}
