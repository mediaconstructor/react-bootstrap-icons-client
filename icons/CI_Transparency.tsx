
"use client";
import React from "react";
import {IconProps, Transparency} from "react-bootstrap-icons";

export const CI_Transparency : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Transparency {...props}/>
        </>
    );
}
