
"use client";
import React from "react";
import {IconProps, Backpack3} from "react-bootstrap-icons";

export const CI_Backpack3 : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Backpack3 {...props}/>
        </>
    );
}
