
"use client";
import React from "react";
import {IconProps, SignYield} from "react-bootstrap-icons";

export const CI_SignYield : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <SignYield {...props}/>
        </>
    );
}
