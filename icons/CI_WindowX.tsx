
"use client";
import React from "react";
import {IconProps, WindowX} from "react-bootstrap-icons";

export const CI_WindowX : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <WindowX {...props}/>
        </>
    );
}
