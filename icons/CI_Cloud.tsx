
"use client";
import React from "react";
import {IconProps, Cloud} from "react-bootstrap-icons";

export const CI_Cloud : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Cloud {...props}/>
        </>
    );
}
