
"use client";
import React from "react";
import {IconProps, XSquare} from "react-bootstrap-icons";

export const CI_XSquare : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <XSquare {...props}/>
        </>
    );
}
