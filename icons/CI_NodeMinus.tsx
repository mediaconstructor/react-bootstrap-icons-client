
"use client";
import React from "react";
import {IconProps, NodeMinus} from "react-bootstrap-icons";

export const CI_NodeMinus : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <NodeMinus {...props}/>
        </>
    );
}
