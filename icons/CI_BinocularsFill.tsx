
"use client";
import React from "react";
import {IconProps, BinocularsFill} from "react-bootstrap-icons";

export const CI_BinocularsFill : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <BinocularsFill {...props}/>
        </>
    );
}
