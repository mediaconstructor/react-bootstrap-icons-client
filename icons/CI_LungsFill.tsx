
"use client";
import React from "react";
import {IconProps, LungsFill} from "react-bootstrap-icons";

export const CI_LungsFill : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <LungsFill {...props}/>
        </>
    );
}
