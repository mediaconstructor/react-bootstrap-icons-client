
"use client";
import React from "react";
import {IconProps, TerminalX} from "react-bootstrap-icons";

export const CI_TerminalX : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <TerminalX {...props}/>
        </>
    );
}
