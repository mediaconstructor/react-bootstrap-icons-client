
"use client";
import React from "react";
import {IconProps, Unindent} from "react-bootstrap-icons";

export const CI_Unindent : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Unindent {...props}/>
        </>
    );
}
