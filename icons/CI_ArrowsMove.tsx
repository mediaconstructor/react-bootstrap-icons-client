
"use client";
import React from "react";
import {IconProps, ArrowsMove} from "react-bootstrap-icons";

export const CI_ArrowsMove : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <ArrowsMove {...props}/>
        </>
    );
}
