
"use client";
import React from "react";
import {IconProps, Unlock} from "react-bootstrap-icons";

export const CI_Unlock : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Unlock {...props}/>
        </>
    );
}
