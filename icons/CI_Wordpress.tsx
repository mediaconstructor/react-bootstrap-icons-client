
"use client";
import React from "react";
import {IconProps, Wordpress} from "react-bootstrap-icons";

export const CI_Wordpress : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Wordpress {...props}/>
        </>
    );
}
