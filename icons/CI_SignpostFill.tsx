
"use client";
import React from "react";
import {IconProps, SignpostFill} from "react-bootstrap-icons";

export const CI_SignpostFill : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <SignpostFill {...props}/>
        </>
    );
}
