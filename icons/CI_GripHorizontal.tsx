
"use client";
import React from "react";
import {IconProps, GripHorizontal} from "react-bootstrap-icons";

export const CI_GripHorizontal : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <GripHorizontal {...props}/>
        </>
    );
}
