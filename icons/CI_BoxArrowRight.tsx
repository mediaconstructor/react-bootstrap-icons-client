
"use client";
import React from "react";
import {IconProps, BoxArrowRight} from "react-bootstrap-icons";

export const CI_BoxArrowRight : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <BoxArrowRight {...props}/>
        </>
    );
}
