
"use client";
import React from "react";
import {IconProps, Escape} from "react-bootstrap-icons";

export const CI_Escape : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Escape {...props}/>
        </>
    );
}
