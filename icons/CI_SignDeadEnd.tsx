
"use client";
import React from "react";
import {IconProps, SignDeadEnd} from "react-bootstrap-icons";

export const CI_SignDeadEnd : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <SignDeadEnd {...props}/>
        </>
    );
}
