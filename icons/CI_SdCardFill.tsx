
"use client";
import React from "react";
import {IconProps, SdCardFill} from "react-bootstrap-icons";

export const CI_SdCardFill : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <SdCardFill {...props}/>
        </>
    );
}
