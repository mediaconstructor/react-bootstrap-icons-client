
"use client";
import React from "react";
import {IconProps, MicMuteFill} from "react-bootstrap-icons";

export const CI_MicMuteFill : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <MicMuteFill {...props}/>
        </>
    );
}
