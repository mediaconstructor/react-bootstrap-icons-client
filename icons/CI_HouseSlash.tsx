
"use client";
import React from "react";
import {IconProps, HouseSlash} from "react-bootstrap-icons";

export const CI_HouseSlash : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <HouseSlash {...props}/>
        </>
    );
}
