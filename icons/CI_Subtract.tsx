
"use client";
import React from "react";
import {IconProps, Subtract} from "react-bootstrap-icons";

export const CI_Subtract : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Subtract {...props}/>
        </>
    );
}
