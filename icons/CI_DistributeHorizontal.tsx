
"use client";
import React from "react";
import {IconProps, DistributeHorizontal} from "react-bootstrap-icons";

export const CI_DistributeHorizontal : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <DistributeHorizontal {...props}/>
        </>
    );
}
