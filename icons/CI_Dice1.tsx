
"use client";
import React from "react";
import {IconProps, Dice1} from "react-bootstrap-icons";

export const CI_Dice1 : React.FC<IconProps> = ({...props}) => {
    return (
        <>
            <Dice1 {...props}/>
        </>
    );
}
